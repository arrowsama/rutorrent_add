#!/bin/bash

#last_commit=$(git rev-parse --verify HEAD | cut -c1-7)
#zip -r xpi/rutorrent_add-0.1.0-$last_commit-fx.xpi content defaults locale modules skin chrome.manifest install.rdf

zip -r xpi/rutorrent_add-fx-last.xpi content defaults locale modules skin chrome.manifest install.rdf